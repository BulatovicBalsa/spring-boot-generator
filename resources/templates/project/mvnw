#!/bin/sh
set -e

BASE_DIR="$(cd "$(dirname "$0")"; pwd)"
WRAPPER_DIR="$BASE_DIR/.mvn/wrapper"
JAR="$WRAPPER_DIR/maven-wrapper.jar"
PROPS="$WRAPPER_DIR/maven-wrapper.properties"

if [ ! -f "$JAR" ]; then
  echo "Missing $JAR. Run: mvn -N wrapper:wrapper"
  exit 1
fi

exec java -jar "$JAR" "$@"